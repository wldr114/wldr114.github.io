<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>给 Hexo 博客移植一个 Neco-Arc 猫猫皮肤</title>
      <link href="/2026/08/17/Neco-Arc-Blog-Skin/"/>
      <url>/2026/08/17/Neco-Arc-Blog-Skin/</url>
      
        <content type="html"><![CDATA[<h2 id="起因"><a href="#起因" class="headerlink" title="起因"></a>起因</h2><p>折腾博客的时候看到一个给 DeepSeek Harness Web 做的皮肤插件 <code>dsh-neco-arc</code>，效果是 6 只 Neco-Arc 猫猫动图漂浮在页面上，可以拖动、缩放、调速，还有一个 mika 头像做角标。觉得很有意思，就想把这套东西搬到自己的 Hexo 博客上。</p><p>这篇文章记录一下从「读别人代码」到「移植到自己博客」的完整过程，以及踩的几个坑。</p><h2 id="拆解-dsh-neco-arc-的实现"><a href="#拆解-dsh-neco-arc-的实现" class="headerlink" title="拆解 dsh-neco-arc 的实现"></a>拆解 dsh-neco-arc 的实现</h2><p>先读了一遍源码，它的结构很清晰：</p><ul><li><strong>host 侧</strong>（<code>lib/index.js</code>）：起一个静态资源路由 <code>/neco-arc/*</code>，只放行白名单里的文件（<code>1.gif</code>~&#96;6.gif<code>、</code>mika.jpg<code>、精灵图等），顺手把页面 favicon 换成 </code>mika.jpg&#96;。</li><li><strong>client 侧</strong>（<code>client/client.js</code>）：注入一段 <code>&lt;style&gt;</code> + 用 React 在几个 slot 里渲染动图层、设置面板、侧栏开关。</li></ul><p>核心思路有三层：</p><ol><li><strong>背景</strong>：整页背景图 + 半透明 token，让皮肤氛围透出来。</li><li><strong>精灵图</strong>：6 张动图不是直接放 GIF，而是用 <strong>PNG 精灵图（sprite sheet）+ CSS <code>steps()</code></strong> 播放，这样才能调播放速度（GIF 的速度是写死的，改不了）。</li><li><strong>交互</strong>：拖拽用 Pointer Events 的 <code>setPointerCapture</code>，位置&#x2F;大小&#x2F;速度&#x2F;可见性全部存 <code>localStorage</code>。</li></ol><p>精灵图播放的原理就是：</p><ul><li>每只猫的动画帧水平拼成一张长图（比如 1 号猫 10 帧拼成 1440×163 的 PNG）。</li><li>用一个 <code>div</code> 把这张长图设为背景，<code>background-size</code> 拉成 <code>帧数 × 单帧宽</code>。</li><li>通过移动 <code>background-position</code> 来切换显示哪一帧。</li></ul><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 移动 background-position 切帧 */</span></span><br><span class="line"><span class="keyword">@keyframes</span> nc-frames &#123;</span><br><span class="line">  <span class="selector-tag">to</span> &#123; <span class="attribute">background-position</span>: <span class="built_in">var</span>(--nc-shift, <span class="number">0</span>) <span class="number">0</span>; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="移植到-Hexo"><a href="#移植到-Hexo" class="headerlink" title="移植到 Hexo"></a>移植到 Hexo</h2><p>我的博客是 Hexo + archer 主题，没有 React 那套 slot 机制，所以直接用<strong>原生 JS</strong> 重写了一遍客户端逻辑，主题模板里只注入一个 <code>&lt;script&gt;</code> 和一个 <code>&lt;link&gt;</code>。</p><p>资源复制到 <code>source/neco-arc/</code> 下，Hexo 会原样拷进 <code>public/</code>，访问路径就是 <code>/neco-arc/xxx</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">source/neco-arc/</span><br><span class="line">├── 1.sheet.png … 6.sheet.png   # 精灵图（可调速）</span><br><span class="line">├── mika.jpg                     # 角标 + favicon</span><br><span class="line">├── neco-arc.css                 # 漂浮动画 + 控制按钮样式</span><br><span class="line">└── neco-arc.js                  # 全部交互逻辑</span><br></pre></td></tr></table></figure><p>每个动图的数据配置长这样：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> <span class="variable constant_">SPRITES</span> = [</span><br><span class="line">  &#123; <span class="attr">key</span>: <span class="string">&#x27;nc-1&#x27;</span>, <span class="attr">src</span>: <span class="string">&#x27;1.sheet.png&#x27;</span>, <span class="attr">cls</span>: <span class="string">&#x27;nc-1&#x27;</span>, <span class="attr">w</span>: <span class="number">92</span>, <span class="attr">frames</span>: <span class="number">10</span>, <span class="attr">ar</span>: <span class="number">1.1319</span>, <span class="attr">totalMs</span>: <span class="number">1000</span> &#125;,</span><br><span class="line">  <span class="comment">// w=显示宽度, frames=帧数, ar=高宽比, totalMs=完整循环时长</span></span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><p><code>frames</code> 决定切几帧，<code>totalMs</code> 决定完整循环多久，两者一起算出速度。</p><h2 id="踩坑记录"><a href="#踩坑记录" class="headerlink" title="踩坑记录"></a>踩坑记录</h2><h3 id="坑一：GIF-调不了速"><a href="#坑一：GIF-调不了速" class="headerlink" title="坑一：GIF 调不了速"></a>坑一：GIF 调不了速</h3><p>一开始图省事直接放 GIF，结果发现 GIF 的播放速度是文件里写死的，CSS&#x2F;JS 都改不了。要调速只能换成精灵图自己控制切帧节奏。</p><h3 id="坑二：CSS-动画的兼容性"><a href="#坑二：CSS-动画的兼容性" class="headerlink" title="坑二：CSS 动画的兼容性"></a>坑二：CSS 动画的兼容性</h3><p>最早照搬插件的 CSS <code>steps()</code> + <code>@keyframes</code> 方案，本地和线上 DOM 检查都正常（背景图、尺寸、动画名都在），但实际渲染就是出不来图。最后改成 **<code>requestAnimationFrame</code> 直接驱动 <code>background-position</code>**，彻底不依赖 CSS 动画，问题解决：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">tick</span>(<span class="params">now</span>) &#123;</span><br><span class="line">  <span class="variable constant_">SPRITES</span>.<span class="title function_">forEach</span>(<span class="keyword">function</span> (<span class="params">s</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> rec = itemEls[s.<span class="property">key</span>]</span><br><span class="line">    <span class="keyword">if</span> (!rec || rec.<span class="property">hidden</span>) <span class="keyword">return</span></span><br><span class="line">    <span class="keyword">var</span> frameMs = s.<span class="property">totalMs</span> / s.<span class="property">frames</span> / state.<span class="property">speed</span></span><br><span class="line">    <span class="keyword">if</span> (now - rec.<span class="property">lastTime</span> &gt;= frameMs) &#123;</span><br><span class="line">      rec.<span class="property">frameIndex</span> = (rec.<span class="property">frameIndex</span> + <span class="number">1</span>) % s.<span class="property">frames</span></span><br><span class="line">      rec.<span class="property">face</span>.<span class="property">style</span>.<span class="property">backgroundPosition</span> = (-rec.<span class="property">frameIndex</span> * rec.<span class="property">dw</span>) + <span class="string">&#x27;px 0px&#x27;</span></span><br><span class="line">      rec.<span class="property">lastTime</span> = now</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="title function_">requestAnimationFrame</span>(tick)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>一个 rAF 循环统一驱动 6 只猫，<code>state.speed</code> 每帧实时读取，改速度立即生效。</p><h3 id="坑三：localStorage-残留状态"><a href="#坑三：localStorage-残留状态" class="headerlink" title="坑三：localStorage 残留状态"></a>坑三：localStorage 残留状态</h3><p>有一版做了个设置面板，里面能「全部隐藏」。后来把面板删了，结果用户强刷后所有动图都不见了——因为旧的 <code>localStorage</code> 里存着 <code>visible: false</code>，新版还在读。解决方式是<strong>给存储 key 加版本号</strong>（<code>neco-arc.state.v3</code>），旧状态直接作废。</p><h3 id="坑四：藏起来就找不回来"><a href="#坑四：藏起来就找不回来" class="headerlink" title="坑四：藏起来就找不回来"></a>坑四：藏起来就找不回来</h3><p>动图 <code>×</code> 掉或者拖出屏幕之后，悬停不到它就没法操作。于是加了一个固定左下角的「恢复 ARC」按钮，一键把所有动图&#x2F;角标复位（显示、位置、大小、速度全回默认）。</p><h2 id="最终效果"><a href="#最终效果" class="headerlink" title="最终效果"></a>最终效果</h2><ul><li><strong>6 只 Neco-Arc 动图 + 1 个 mika 角标</strong> 漂浮在博客页面上，位置沿边缘排布，不挡正文。</li><li>每个元素都能：<strong>拖动</strong>改位置、**<code>+</code>&#x2F;<code>−</code>** 缩放、**<code>1×</code>** 循环调速（0.5×→3×）、**<code>×</code>** 隐藏。</li><li><strong>「恢复 ARC」按钮</strong> 一键复位。</li><li>所有状态存 <code>localStorage</code>，刷新不丢。</li><li>favicon 换成了 mika，右下角有 mika 圆形角标。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>整个过程其实就是「读一个成熟实现 → 抽离核心思路 → 用更简单的技术栈重写 → 踩坑 → 修」。几个体会：</p><ol><li>精灵图 + 手动切帧是控制动画速度的关键，直接上 GIF 就锁死了。</li><li>CSS 动画很优雅，但遇到兼容性问题时，<code>requestAnimationFrame</code> 手动驱动是最稳的兜底。</li><li>前端状态持久化一定要考虑「版本迁移」，否则改版后老数据会反过来坑你。</li><li>所有「能藏起来」的东西，都得留一个「能找回来」的入口。</li></ol><p>代码都在博客仓库里，感兴趣可以看看 <code>source/neco-arc/</code> 下的实现。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Hexo </tag>
            
            <tag> 前端 </tag>
            
            <tag> JavaScript </tag>
            
            <tag> CSS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>学习回顾：从 C 语言到二进制安全</title>
      <link href="/2026/05/19/Learning-Retrospective/"/>
      <url>/2026/05/19/Learning-Retrospective/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>学了不少东西，东一块西一块的，趁这个机会整理一下自己到底学了什么，也梳理下以后的方向。这篇算是第二篇正经博客，之前那篇 First Blog 随便写的，不用管它。</p><hr><h2 id="C-C-：从语法到-STL，再到啃不动的书"><a href="#C-C-：从语法到-STL，再到啃不动的书" class="headerlink" title="C&#x2F;C++：从语法到 STL，再到啃不动的书"></a>C&#x2F;C++：从语法到 STL，再到啃不动的书</h2><p>C&#x2F;C++ 是最早系统学的语言，从最基本的语法（指针、内存管理、结构体、类）到 STL 容器（<code>vector</code>、<code>map</code>、<code>set</code>、<code>queue</code>、<code>stack</code>、<code>priority_queue</code> 这些）都过了一遍。STL 不止容器，还有 <code>&lt;algorithm&gt;</code> 里的 <code>sort</code>、<code>lower_bound</code>、<code>next_permutation</code> 等等，刷题常用；<code>&lt;string&gt;</code> 和 <code>&lt;sstream&gt;</code> 做字符串处理也很方便；<code>&lt;functional&gt;</code> 配合 lambda 写起来很顺手。</p><p>书也读了几本：</p><ul><li><strong>《C++ Primer》</strong>：经典教材，跟着过了语法和基础特性，但老实说没完全消化，以后当手册翻。</li><li><strong>《Effective C++》</strong>：Scott Meyers 那本，讲了 55 个改善 C++ 编程的建议。难，真的难看懂，很多条款涉及模板、继承体系、异常安全这些深层机制，当时功力不够，只能先混个眼熟。</li><li><strong>《数据结构与算法分析（C 语言描述）》</strong>：这本应该是四大件之一了，也是最感兴趣的一本。树（AVL、红黑树、B 树），图（邻接表&#x2F;邻接矩阵、拓扑排序、最短路径），还有各种散列和堆，深度优先（DFS）和广度优先（BFS）这些算法，写起来很有意思。</li><li><strong>《算法导论》（CLRS）</strong>：砖头书，没通读，当参考书翻了一些章节，主要还是配合数据结构那本一起看。</li></ul><hr><h2 id="Python：从脚本小子到安全工具链"><a href="#Python：从脚本小子到安全工具链" class="headerlink" title="Python：从脚本小子到安全工具链"></a>Python：从脚本小子到安全工具链</h2><p>Python 语法学得比较快，算是上手成本最低的语言。真正用起来是学安全之后：</p><h3 id="爬虫-信息搜集"><a href="#爬虫-信息搜集" class="headerlink" title="爬虫 &amp; 信息搜集"></a>爬虫 &amp; 信息搜集</h3><ul><li>**<code>requests</code>**：发 HTTP 请求，模拟登录、抓页面、构造 payload。</li><li>**<code>beautifulsoup4</code>**：解析 HTML，提取表单、链接、隐藏字段。</li><li>**<code>re</code>**：正则匹配，从响应里抠 flag、找敏感信息。</li></ul><h3 id="GUI-开发"><a href="#GUI-开发" class="headerlink" title="GUI 开发"></a>GUI 开发</h3><p>用 <code>tkinter</code> 和 <code>PyQt</code> 写过小工具，给一些安全脚本套个图形界面，不用每次敲命令行。</p><h3 id="PWN-方向"><a href="#PWN-方向" class="headerlink" title="PWN 方向"></a>PWN 方向</h3><p>Python + pwntools 是标配。<code>from pwn import *</code> 一行搞定远程连接、构造 payload、格式化和调试输出。libcsearch 之类的库用来查 libc 版本和偏移，做 ret2libc、one_gadget 都需要。</p><hr><h2 id="Java：基本语法-Android-逆向起步"><a href="#Java：基本语法-Android-逆向起步" class="headerlink" title="Java：基本语法 + Android 逆向起步"></a>Java：基本语法 + Android 逆向起步</h2><p>Java 学了基本语法（面向对象、集合框架、IO），主要用得上的场景是 Android 逆向。碰到过安卓 Java 层的逆向题目，用 <code>jadx</code> 反编译 APK 看源码、分析逻辑、找加密函数入口。对 Smali 也有初步了解。</p><p>Java 的语法和 C++ 接近但又更干净（没有指针那套），做项目比 C++ 省心很多，之后打算用 Java 做点实际的东西。</p><hr><h2 id="汇编：底层的语言"><a href="#汇编：底层的语言" class="headerlink" title="汇编：底层的语言"></a>汇编：底层的语言</h2><p>汇编属于必须掌握但不需要精通的程度。x86&#x2F;x64 指令集基本能看懂，<code>mov</code>、<code>lea</code>、<code>call</code>、<code>ret</code>、<code>push</code>、<code>pop</code> 这些，配合 call convention（cdecl、stdcall、fastcall、System V AMD64 ABI）看反汇编代码。对 PWN 题来说，读汇编是基本功，特别是分析栈帧布局、找返回地址偏移。</p><hr><h2 id="其他语言：广撒网，浅尝辄止"><a href="#其他语言：广撒网，浅尝辄止" class="headerlink" title="其他语言：广撒网，浅尝辄止"></a>其他语言：广撒网，浅尝辄止</h2><table><thead><tr><th>语言</th><th>掌握程度</th><th>说明</th></tr></thead><tbody><tr><td>Rust</td><td>略有涉猎</td><td>安全圈热门，owner 机制很特别</td></tr><tr><td>Go</td><td>略有涉猎</td><td>并发模型简洁，写工具很合适</td></tr><tr><td>HTML&#x2F;CSS</td><td>略有涉猎</td><td>懂基础能写页面，也了解 XSS 需要</td></tr><tr><td>JavaScript</td><td>略有涉猎</td><td>前端相关，XSS 核心</td></tr><tr><td>PHP</td><td>略有涉猎</td><td>审计过简单代码，文件上传也涉及</td></tr></tbody></table><hr><h2 id="操作系统-Linux"><a href="#操作系统-Linux" class="headerlink" title="操作系统 &amp; Linux"></a>操作系统 &amp; Linux</h2><p>操作系统方面基本专注 Linux，毕竟大部分工具链和安全实验都在 Linux 下跑。</p><ul><li>**<a href="https://pwn.college/">pwn.college</a>**：ASU 的开源 CTF 平台，从 Linux 基础到高级二进制漏洞利用，循序渐进，质量很高。在上面刷了不少模块。</li><li><strong>网络测试</strong>：用过 Wireshark 抓包分析流量，Maltego 做信息关联和 OSCINT 侦察。</li><li><strong>二进制调试工具链</strong>：<ul><li><code>pwndbg</code>：GDB 的 PWN 增强插件，heap 可视化、context 自动显示、ROPgadget 查找，日用必备。</li><li><code>GDB</code>：原版 GNU Debugger，配合 <code>gef</code>、<code>peda</code> 也用过。</li><li><code>objdump</code> &#x2F; <code>readelf</code>：静态分析二进制文件结构和反汇编。</li><li><code>checksec</code>：检查二进制保护机制（PIE、NX、Canary、RELRO、FORTIFY）。</li></ul></li></ul><hr><h2 id="安全方向：杂七杂八的经验"><a href="#安全方向：杂七杂八的经验" class="headerlink" title="安全方向：杂七杂八的经验"></a>安全方向：杂七杂八的经验</h2><h3 id="Web-安全"><a href="#Web-安全" class="headerlink" title="Web 安全"></a>Web 安全</h3><ul><li><strong>SQL 注入</strong>：在 SQL-Lab 平台逐关复现过，从 Union 注入、报错注入、布尔盲注到时间盲注，再到二次注入、堆叠注入，基本把常见类型过了一遍。</li><li><strong>Burp Suite</strong>：爆破、抓包改包、Intruder 模块配合字典跑弱口令。</li><li><strong>Apache</strong>：简单了解过 Apache 配置和常见漏洞（目录遍历、文件解析漏洞）。</li></ul><h3 id="逆向工程（Reverse）"><a href="#逆向工程（Reverse）" class="headerlink" title="逆向工程（Reverse）"></a>逆向工程（Reverse）</h3><ul><li><strong>花指令</strong>：反汇编干扰，通过修改跳转绕过。</li><li><strong>平铺（代码平坦化）</strong>：控制流混淆，用符号执行或动调追踪还原逻辑。</li><li><strong>加解密算法识别</strong>：常见的 base64、AES、DES、RC4、TEA、XXTEA，能识别特征并逆向还原（或写出解密脚本）。</li></ul><h3 id="PWN-二进制漏洞利用"><a href="#PWN-二进制漏洞利用" class="headerlink" title="PWN &#x2F; 二进制漏洞利用"></a>PWN &#x2F; 二进制漏洞利用</h3><ul><li><strong>栈溢出</strong>：最经典的攻击面，覆盖返回地址 → ret2text → ret2shellcode → ret2libc → ret2csu → ret2dlresolve，逐渐深入。</li><li><strong>堆溢出</strong>：ptmalloc2 的内存管理机制（bin、tcache、fastbin），<code>use after free</code>、<code>double free</code>、<code>off-by-one</code>、<code>unsorted bin attack</code>、<code>tcache poisoning</code> 等利用手法。</li><li><strong>内核</strong>：目前还在入门阶段，了解 LKM、漏洞触发、<code>commit_creds(prepare_kernel_cred(0))</code> 这个经典提权链，还有 KPTI、SMEP&#x2F;SMAP 这些缓解机制。</li></ul><hr><h2 id="以后的方向"><a href="#以后的方向" class="headerlink" title="以后的方向"></a>以后的方向</h2><p>学了不少，但很多都是蜻蜓点水。接下来想重点深入：</p><ol><li><strong>Web 安全</strong>：把 XSS（反射型、存储型、DOM 型）吃透，CSP 绕过也要研究。Linux 渗透测试的完整流程从信息收集到提权。</li><li><strong>逆向</strong>：Android 逆向（so 层 Native 逆向，Frida hook），macOS&#x2F;iOS 逆向有兴趣但不急。</li><li><strong>二进制</strong>：PWN 往内核方向深入，堆利用再巩固，浏览器漏洞方向也可以了解。</li><li><strong>开发</strong>：用 Java 做一两个完整的项目出来，实战练手。Go 写安全工具作为辅助语言。</li></ol><p>路还很长，坚持写博客记录吧。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Git 入门：从 clone 到 push</title>
      <link href="/2026/05/19/Git-Getting-Started/"/>
      <url>/2026/05/19/Git-Getting-Started/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>今天开始捣鼓 Git 和 GitHub  写点项目、学好版本控制</p><h2 id="成果展示"><a href="#成果展示" class="headerlink" title="成果展示"></a>成果展示</h2><p>先贴一张今天的成果图</p><p><img src="/pt/git.png" alt="Git 操作截图"></p><h2 id="Git-工作流实战"><a href="#Git-工作流实战" class="headerlink" title="Git 工作流实战"></a>Git 工作流实战</h2><p>操作流程</p><h3 id="1-克隆仓库到本地"><a href="#1-克隆仓库到本地" class="headerlink" title="1. 克隆仓库到本地"></a>1. 克隆仓库到本地</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/wldr114/school-lab-experiments</span><br><span class="line"><span class="comment"># 把远程仓库完整复制到本地，会自动创建本地目录</span></span><br></pre></td></tr></table></figure><h3 id="2-创建并切换到新分支"><a href="#2-创建并切换到新分支" class="headerlink" title="2. 创建并切换到新分支"></a>2. 创建并切换到新分支</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git checkout -b my-feature</span><br><span class="line"><span class="comment"># -b 表示创建新分支并切换过去</span></span><br><span class="line"><span class="comment"># my-feature 是分支名，开发新功能最好别直接在 main 上改</span></span><br></pre></td></tr></table></figure><h3 id="3-查看改动"><a href="#3-查看改动" class="headerlink" title="3. 查看改动"></a>3. 查看改动</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git diff</span><br><span class="line"><span class="comment"># 查看工作区相对于暂存区的具体改动内容</span></span><br><span class="line"><span class="comment"># 用来确认自己改了什么，很实用的命令</span></span><br></pre></td></tr></table></figure><h3 id="4-暂存所有改动"><a href="#4-暂存所有改动" class="headerlink" title="4. 暂存所有改动"></a>4. 暂存所有改动</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git add .</span><br><span class="line"><span class="comment"># . 表示把当前目录下所有改动加入暂存区（staging area）</span></span><br><span class="line"><span class="comment"># git add 之后，改动才算&quot;准备提交&quot;的状态</span></span><br></pre></td></tr></table></figure><h3 id="5-提交改动"><a href="#5-提交改动" class="headerlink" title="5. 提交改动"></a>5. 提交改动</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;更新 README.md&quot;</span></span><br><span class="line"><span class="comment"># -m 后面跟提交信息，描述这次改了什么</span></span><br><span class="line"><span class="comment"># 养成写清楚 commit message 的习惯很重要</span></span><br></pre></td></tr></table></figure><h3 id="6-推送到远程仓库"><a href="#6-推送到远程仓库" class="headerlink" title="6. 推送到远程仓库"></a>6. 推送到远程仓库</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">git push -u origin my-feature</span><br><span class="line"><span class="comment"># -u 表示把本地分支和远程分支关联起来（upstream）</span></span><br><span class="line"><span class="comment"># origin 是远程仓库的默认名称</span></span><br><span class="line"><span class="comment"># my-feature 是要推送的分支名</span></span><br><span class="line"><span class="comment"># 关联之后，下次直接 git push 就可以</span></span><br></pre></td></tr></table></figure><h2 id="一些疑问：git-push-f-危不危险？"><a href="#一些疑问：git-push-f-危不危险？" class="headerlink" title="一些疑问：git push -f 危不危险？"></a>一些疑问：<code>git push -f</code> 危不危险？</h2><p><code>git push -f</code>（即 <code>git push --force</code>）会强行用本地的提交历史覆盖远程分支……</p><h2 id="今日总结"><a href="#今日总结" class="headerlink" title="今日总结"></a>今日总结</h2><p>学习了 Git 的基本工作流clone → 建分支 → 改代码 → diff 检查 → add 暂存 → commit 提交 → push 推送，这是日常开发的标准流程</p><p>以后坚持每天写代码、写博客，慢慢积累</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Frist Blog</title>
      <link href="/2026/05/08/Frist%20Blog/"/>
      <url>/2026/05/08/Frist%20Blog/</url>
      
        <content type="html"><![CDATA[<h1 id="我的第一篇博客"><a href="#我的第一篇博客" class="headerlink" title="我的第一篇博客"></a>我的第一篇博客</h1><ul><li>Web 安全学习</li><li>CTF 做题笔记</li><li>Java 基础</li><li>Linux 和工具使用</li></ul><h2 id="最近在学什么"><a href="#最近在学什么" class="headerlink" title="最近在学什么"></a>最近在学什么</h2><p>最近在重新学习 Web 安全，主要了解了：</p><ul><li>文件上传漏洞</li><li>JWT 基础</li><li>BurpSuite 的简单使用</li><li>Java 审计入门</li></ul><p>虽然现在还比较菜，不过希望以后能坚持更新。</p><h2 id="关于这个博客"><a href="#关于这个博客" class="headerlink" title="关于这个博客"></a>关于这个博客</h2><p>这个博客使用 Hexo 搭建，部署在 GitHub Pages 上。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2024/12/28/hello-world/"/>
      <url>/2024/12/28/hello-world/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
